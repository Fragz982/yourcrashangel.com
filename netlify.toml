[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"

# Serverless function that proxies the AI photo-to-estimate provider, keeping
# the API key server-side. Dormant (returns configured:false) until the
# ESTIMATE_API_KEY env var is set — the client falls back to the guided ballpark.
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# Security headers applied to every response. CSP intentionally allows
# 'unsafe-inline' for scripts/styles because the static export ships inline
# hydration scripts and framer-motion injects inline styles; this still blocks
# external script/object injection. img-src allows Unsplash (placeholder photos).
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), interest-cohort=()"
    Strict-Transport-Security = "max-age=31536000; includeSubDomains"
    Content-Security-Policy = "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https://images.unsplash.com; media-src 'self' blob:; font-src 'self'; connect-src 'self' https://vpic.nhtsa.dot.gov; frame-ancestors 'none'; base-uri 'self'; form-action 'self'; object-src 'none'"
